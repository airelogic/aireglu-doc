import{_ as s,c as e,o as a,a4 as n}from"./chunks/framework.ttppV29E.js";const m=JSON.parse('{"title":"Liquid Specification Differences","description":"","frontmatter":{},"headers":[],"relativePath":"docs/quickstart/liquidspec.md","filePath":"docs/quickstart/liquidspec.md","lastUpdated":1786547185000}'),i={name:"docs/quickstart/liquidspec.md"},t=n(`<h1 id="liquid-specification-differences" tabindex="-1">Liquid Specification Differences <a class="header-anchor" href="#liquid-specification-differences" aria-label="Permalink to &quot;Liquid Specification Differences&quot;">​</a></h1><p>AireGlu implements the Shopify Liquid specification as much as possible, but due to AireGlu-specific requirements and different underlying technologies, there are some differences. This page will focus on the differences between the Shopify Liquid specification and our own implementation. We will also highlight differences between our Liquid engine versions.</p><ul><li><a href="#differences-compared-to-shopify-liquid-documentation">Differences Compared to Shopify Liquid Documentation</a></li><li><a href="#v1-and-v2-differences">V1 and V2 Differences</a></li></ul><h2 id="differences-compared-to-shopify-liquid-documentation" tabindex="-1">Differences Compared to Shopify Liquid Documentation <a class="header-anchor" href="#differences-compared-to-shopify-liquid-documentation" aria-label="Permalink to &quot;Differences Compared to Shopify Liquid Documentation&quot;">​</a></h2><h3 id="basics" tabindex="-1">Basics <a class="header-anchor" href="#basics" aria-label="Permalink to &quot;Basics&quot;">​</a></h3><h4 id="operators" tabindex="-1">Operators <a class="header-anchor" href="#operators" aria-label="Permalink to &quot;Operators&quot;">​</a></h4><h5 id="contains" tabindex="-1"><code>contains</code> <a class="header-anchor" href="#contains" aria-label="Permalink to &quot;\`contains\`&quot;">​</a></h5><p>The Shopify Liquid documentation mentions that <code>contains</code> allows you to also check for the presence of a string in an array of strings. However, this is not possible for either version of our Liquid engine. Therefore, instead of doing:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% if product.tags contains &quot;Hello&quot; %}</span></span>
<span class="line"><span>  This product has been tagged with &quot;Hello&quot;.</span></span>
<span class="line"><span>{% endif %}</span></span></code></pre></div><p>You will need to do something like this instead:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% assign has_hello = false %}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{% for tag in input.product.tags %}</span></span>
<span class="line"><span>  {% if tag == &quot;Hello&quot; %}</span></span>
<span class="line"><span>    {% assign has_hello = true %}</span></span>
<span class="line"><span>  {% endif %}</span></span>
<span class="line"><span>{% endfor %}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{% if has_hello %}</span></span>
<span class="line"><span>  This product has been tagged with &quot;Hello&quot;.</span></span>
<span class="line"><span>{% endif %}</span></span></code></pre></div><h4 id="truthy-and-falsy" tabindex="-1">Truthy and Falsy <a class="header-anchor" href="#truthy-and-falsy" aria-label="Permalink to &quot;Truthy and Falsy&quot;">​</a></h4><p>The Shopify Liquid documentation states that arrays and empty arrays are truthy. However, this is not the case in AireGlu and you will not be able to use these in conditional checks. Given the following Liquid:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% if [] or [&quot;test&quot;] %}</span></span>
<span class="line"><span>  This is invalid AireGlu Liquid.</span></span>
<span class="line"><span>{% endif %}</span></span></code></pre></div><p>You will simply just get:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% if [] or [&quot;test&quot;] %}</span></span>
<span class="line"><span>  This is invalid AireGlu Liquid.</span></span>
<span class="line"><span>{% endif %}</span></span></code></pre></div><p>returned back to you instead of <code>This is invalid AireGlu Liquid.</code>.</p><p>For more information on Truthy and Falsy, please see the <a href="https://shopify.github.io/Liquid/basics/truthy-and-falsy/" target="_blank" rel="noreferrer">Shopify Liquid documentation</a>.</p><h4 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h4><h5 id="strings" tabindex="-1">Strings <a class="header-anchor" href="#strings" aria-label="Permalink to &quot;Strings&quot;">​</a></h5><p>The Shopify Liquid documentation states that Liquid does not convert escape sequences (such as <code>\\n</code>) into special characters. However, AireGlu does convert escape sequences into special characters, for both Liquid engine versions. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>STRING:</span></span>
<span class="line"><span>{% assign my_string = &quot;Hello World!&quot; %}</span></span>
<span class="line"><span>{% assign my_string_escape = &quot;Hello\\n \\&quot;World\\&quot;!&quot; %}</span></span>
<span class="line"><span>{{ my_string }}</span></span>
<span class="line"><span>{{ my_string_escape }}</span></span></code></pre></div><p>will give you the following result:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>STRING:</span></span>
<span class="line"><span>Hello World!</span></span>
<span class="line"><span>Hello</span></span>
<span class="line"><span> &quot;World&quot;!</span></span></code></pre></div><h5 id="array" tabindex="-1">Array <a class="header-anchor" href="#array" aria-label="Permalink to &quot;Array&quot;">​</a></h5><h6 id="accessing-items-in-arrays" tabindex="-1">Accessing items in arrays <a class="header-anchor" href="#accessing-items-in-arrays" aria-label="Permalink to &quot;Accessing items in arrays&quot;">​</a></h6><p>The Shopify Liquid documentation shows the following Liquid:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% for user in site.users %}</span></span>
<span class="line"><span>  {{ user }}</span></span>
<span class="line"><span>{% endfor %}</span></span></code></pre></div><p>Resulting in:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  Tobi Laura Tetsuro Adam</span></span></code></pre></div><p>Although, in AireGlu these will each get a newline appended e.g. for V1 you will get:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  Tobi</span></span>
<span class="line"><span>  Laura</span></span>
<span class="line"><span>  Tetsuro</span></span>
<span class="line"><span>  Adam</span></span></code></pre></div><p>And for V2 you will get:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span></span></span>
<span class="line"><span>  Tobi</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  Laura</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  Tetsuro</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  Adam</span></span></code></pre></div><p>Therefore, you will need to make use of hyphens which are documented in the Whitespace Control section in order to get the desired result of all of the users being on a single line.</p><h6 id="accessing-specific-items-in-arrays" tabindex="-1">Accessing specific items in arrays <a class="header-anchor" href="#accessing-specific-items-in-arrays" aria-label="Permalink to &quot;Accessing specific items in arrays&quot;">​</a></h6><p>The Shopify Liquid documentation mentions that a negative index will count from the end of the array. e.g. <span><code>{{ site.users[-1] }}</code></span> would give you the last item of the array. However, in AireGlu this is not the case and using a <code>-1</code> index will give you an empty result.</p><p>For more information on Types, please see the <a href="https://shopify.github.io/Liquid/basics/types/" target="_blank" rel="noreferrer">Shopify Liquid documentation</a>.</p><h3 id="tags" tabindex="-1">Tags <a class="header-anchor" href="#tags" aria-label="Permalink to &quot;Tags&quot;">​</a></h3><h4 id="template" tabindex="-1">Template <a class="header-anchor" href="#template" aria-label="Permalink to &quot;Template&quot;">​</a></h4><p>The following do not work in either of our Liquid engine versions:</p><ul><li><code>inline comments</code></li><li><code>Liquid</code></li><li><code>render</code></li><li><code>include</code></li></ul><h3 id="filters" tabindex="-1">Filters <a class="header-anchor" href="#filters" aria-label="Permalink to &quot;Filters&quot;">​</a></h3><h4 id="capitalize" tabindex="-1"><code>capitalize</code> <a class="header-anchor" href="#capitalize" aria-label="Permalink to &quot;\`capitalize\`&quot;">​</a></h4><p>In the Shopify Liquid documentation, the following:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{{ &quot;my GREAT title&quot; | capitalize }}</span></span></code></pre></div><p>claims to result in <code>My great title</code>, however, both of our Liquid engine versions give us <code>My GREAT Title</code>. In other words the first character is capitalised but the rest of the string remains the same.</p><h4 id="size" tabindex="-1"><code>size</code> <a class="header-anchor" href="#size" aria-label="Permalink to &quot;\`size\`&quot;">​</a></h4><p>Size works as expected for both of our Liquid engine versions, the only difference is when trying to do something like <span><code>{{ &quot;this is a string&quot;.size }}</code></span>. This won&#39;t work as the Shopify Liquid documentation mentions, however you can work around this by doing <span><code>{{ &quot;this is a string&quot; | size }}</code></span>. However, if you were to access a string via the input for example (instead of using a direct string) then you would be able to use the <code>size</code> filter.</p><p>For example, given the input of:</p><div class="language-xml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">xml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">nametwo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;Fox Mulder&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">nametwo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p><span><code>{{ input.nametwo.size }}</code></span> and <span><code>{{ input.nametwo | size }}</code></span> both correctly give you <code>10</code>, the number of characters in <code>Fox Mulder</code>. This works in both Liquid engine versions.</p><h5 id="first-and-last-on-plain-strings" tabindex="-1"><code>first</code> and <code>last</code> on plain strings <a class="header-anchor" href="#first-and-last-on-plain-strings" aria-label="Permalink to &quot;\`first\` and \`last\` on plain strings&quot;">​</a></h5><p>Given the same <code>&lt;nametwo&gt;Fox Mulder&lt;/nametwo&gt;</code> example, <span><code>{{ input.nametwo.first }}</code></span> and <span><code>{{ input.nametwo.last }}</code></span> give you the first and last characters, <code>F</code> and <code>r</code> - but <strong>only in V2</strong>. <strong>V1 does not support <code>.first</code>/<code>.last</code> on strings at all</strong> - this returns nothing for any string, not just ones that came from XML. <code>.size</code> above is unaffected by this and works correctly in both versions.</p><h5 id="an-xml-element-with-an-attribute" tabindex="-1">An XML element with an attribute <a class="header-anchor" href="#an-xml-element-with-an-attribute" aria-label="Permalink to &quot;An XML element with an attribute&quot;">​</a></h5><p>If the XML element you are accessing has an attribute on it, it is not treated as a plain string internally even if its only content is text. Given:</p><div class="language-xml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">xml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;Fox Mulder&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p><span><code>{{ input.name }}</code></span> still gives you <code>Fox Mulder</code>, and <span><code>{{ input.name.id }}</code></span> gives you <code>1</code>, in both Liquid engine versions.</p><p><strong>V2</strong> also correctly treats the value as a string for everything else, provided the element has no <em>child elements</em> of its own (an attribute alone does not stop this working) - exactly matching the plain-string behaviour described above:</p><ul><li><span><code>{{ input.name.size }}</code></span> gives you <code>10</code>, the number of characters in <code>Fox Mulder</code>.</li><li><span><code>{{ input.name.first }}</code></span> and <span><code>{{ input.name.last }}</code></span> give you <code>F</code> and <code>r</code>, the first and last characters.</li><li><span><code>{% if input.name == &quot;Fox Mulder&quot; %}</code></span> correctly evaluates to <strong>true</strong>.</li></ul><p><strong>V1</strong> does not have this - <code>.size</code>, <code>.first</code>, <code>.last</code> all give you nothing, and <code>==</code> always evaluates to <strong>false</strong>, even though <code>input.name</code> prints as <code>Fox Mulder</code>. Note this is on top of the <code>.first</code>/<code>.last</code> limitation above that affects V1 regardless of attributes - even once you work around the attribute, <code>.first</code>/<code>.last</code> still won&#39;t work in V1. If you need this to work the same way in V1, force the value into a plain string first e.g.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% assign name_text = input.name | append: &quot;&quot; %}</span></span>
<span class="line"><span>{{ name_text.size }}</span></span>
<span class="line"><span>{% if name_text == &quot;Fox Mulder&quot; %}&quot;yes&quot;{% else %}&quot;no&quot;{% endif %}</span></span></code></pre></div><p>would give you <code>10</code> and <code>&quot;yes&quot;</code> respectively (<code>name_text.first</code>/<code>.last</code> would still give you nothing in V1, per the limitation above).</p><p>Note that if the element also has child elements of its own (not just an attribute), V2 keeps treating it as a one-item array rather than a string, since that matches the intent of <a href="#xml-single-vs-multiple-elements">XML single vs multiple elements</a> below - the string fallback above only applies to a leaf element (no child elements) that has an attribute.</p><h2 id="v1-and-v2-differences" tabindex="-1">V1 and V2 differences <a class="header-anchor" href="#v1-and-v2-differences" aria-label="Permalink to &quot;V1 and V2 differences&quot;">​</a></h2><h3 id="basics-1" tabindex="-1">Basics <a class="header-anchor" href="#basics-1" aria-label="Permalink to &quot;Basics&quot;">​</a></h3><h4 id="operators-1" tabindex="-1">Operators <a class="header-anchor" href="#operators-1" aria-label="Permalink to &quot;Operators&quot;">​</a></h4><h5 id="order-of-operations" tabindex="-1">Order of operations <a class="header-anchor" href="#order-of-operations" aria-label="Permalink to &quot;Order of operations&quot;">​</a></h5><p>Given the following Liquid:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% if true or false and false %}</span></span>
<span class="line"><span>  This evaluates to true, since the \`and\` condition is checked first.</span></span>
<span class="line"><span>{% endif %}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{% if true and false and false or true %}</span></span>
<span class="line"><span>  This evaluates to false, since the tags are checked like this:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  true and (false and (false or true))</span></span>
<span class="line"><span>  true and (false and true)</span></span>
<span class="line"><span>  true and false</span></span>
<span class="line"><span>  false</span></span>
<span class="line"><span>{% endif %}</span></span></code></pre></div><p>You will get a different output depending on whether your task has the v1 Liquid engine selected or the v2 Liquid engine.</p><p>V1 Output:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ORDER OF OPERATIONS:</span></span>
<span class="line"><span>  This evaluates to true, since the \`and\` condition is checked first.</span></span>
<span class="line"><span>  This evaluates to false, since the tags are checked like this:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  true and (false and (false or true))</span></span>
<span class="line"><span>  true and (false and true)</span></span>
<span class="line"><span>  true and false</span></span>
<span class="line"><span>  false</span></span></code></pre></div><p>V2 Output:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ORDER OF OPERATIONS:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  This evaluates to true, since the \`and\` condition is checked first.</span></span></code></pre></div><p>The V2 version is now inline with what is expected from the Shopify Liquid documentation. In tags with more than one <code>and</code> or <code>or</code> operator, operators are checked in order from right to left.</p><h4 id="truthy-and-falsy-1" tabindex="-1">Truthy and Falsy <a class="header-anchor" href="#truthy-and-falsy-1" aria-label="Permalink to &quot;Truthy and Falsy&quot;">​</a></h4><h5 id="array-1" tabindex="-1">Array <a class="header-anchor" href="#array-1" aria-label="Permalink to &quot;Array&quot;">​</a></h5><p>One thing to note with arrays in V1 compared to V2 is how they handle being compared against non-array values. If you had the following:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>result=[{% if data.myArray == &#39;test&#39; %}EQUAL{% else %}NOT-EQUAL{% endif %}]</span></span></code></pre></div><p>V1 would return</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>result=[EQUAL]</span></span></code></pre></div><p>Whereas V2 would return</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>result=[NOT-EQUAL]</span></span></code></pre></div><p>In other words, V1 would always return true when comparing an array against a non-array value, whereas V2 correctly returns false instead.</p><h4 id="whitespace-control" tabindex="-1">Whitespace Control <a class="header-anchor" href="#whitespace-control" aria-label="Permalink to &quot;Whitespace Control&quot;">​</a></h4><p>Given the following Liquid:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>WHITESPACE CONTROL:</span></span>
<span class="line"><span>{% assign my_variable = &quot;tomato&quot; %}</span></span>
<span class="line"><span>{{ my_variable }}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{% assign my_variable = &quot;tomato&quot; -%}</span></span>
<span class="line"><span>{{ my_variable }}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{% assign username = &quot;John G. Chalmers-Smith&quot; %}</span></span>
<span class="line"><span>{% if username and username.size &gt; 10 %}</span></span>
<span class="line"><span>  Wow, {{ username }} , you have a long name!</span></span>
<span class="line"><span>{% else %}</span></span>
<span class="line"><span>  Hello there!</span></span>
<span class="line"><span>{% endif %}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{% assign username = &quot;John G. Chalmers-Smith&quot; -%}</span></span>
<span class="line"><span>{%- if username and username.size &gt; 10 -%}</span></span>
<span class="line"><span>  Wow, {{ username -}} , you have a long name!</span></span>
<span class="line"><span>{%- else -%}</span></span>
<span class="line"><span>  Hello there!</span></span>
<span class="line"><span>{%- endif %}</span></span></code></pre></div><p>You will get a different output depending on whether your task has the v1 Liquid engine selected or the v2 Liquid engine.</p><p>V1 Output:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>WHITESPACE CONTROL:</span></span>
<span class="line"><span>tomato</span></span>
<span class="line"><span></span></span>
<span class="line"><span>tomato</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  Wow, John G. Chalmers-Smith , you have a long name!</span></span>
<span class="line"><span>  Wow, John G. Chalmers-Smith, you have a long name!</span></span></code></pre></div><p>V2 Output:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>WHITESPACE CONTROL:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>tomato</span></span>
<span class="line"><span></span></span>
<span class="line"><span>tomato</span></span>
<span class="line"><span></span></span>
<span class="line"><span></span></span>
<span class="line"><span></span></span>
<span class="line"><span>  Wow, John G. Chalmers-Smith , you have a long name!</span></span>
<span class="line"><span></span></span>
<span class="line"><span></span></span>
<span class="line"><span>Wow, John G. Chalmers-Smith, you have a long name!</span></span></code></pre></div><p>The V2 version is now inline with what is expected from the Shopify Liquid documentation. Normally, even if it doesn’t print text, any line of Liquid in your template will still print a blank line. Therefore, you will now need to use a hyphen in your tag syntax <span><code>{{-</code>, <code>-}}</code>, <code>{%-,</code> and <code>-%}</code></span> to strip whitespace from the left or right side of a rendered tag.</p><p>So to get the same output as V1 in V2 you would need to do something like this (notice the additional hyphens):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>WHITESPACE CONTROL:</span></span>
<span class="line"><span>{% assign my_variable = &quot;tomato&quot; -%}</span></span>
<span class="line"><span>{{ my_variable }}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{% assign my_variable = &quot;tomato&quot; -%}</span></span>
<span class="line"><span>{{ my_variable -}}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{% assign username = &quot;John G. Chalmers-Smith&quot; %}</span></span>
<span class="line"><span>{% if username and username.size &gt; 10 %}</span></span>
<span class="line"><span>  Wow, {{ username }} , you have a long name!</span></span>
<span class="line"><span>{%- else -%}</span></span>
<span class="line"><span>  Hello there!</span></span>
<span class="line"><span>{% endif -%}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{% assign username = &quot;John G. Chalmers-Smith&quot; -%}</span></span>
<span class="line"><span>{%- if username and username.size &gt; 10 %}</span></span>
<span class="line"><span>  Wow, {{ username -}} , you have a long name!</span></span>
<span class="line"><span>{% else %}</span></span>
<span class="line"><span>  Hello there!</span></span>
<span class="line"><span>{% endif %}</span></span></code></pre></div><h5 id="capture" tabindex="-1">Capture <a class="header-anchor" href="#capture" aria-label="Permalink to &quot;Capture&quot;">​</a></h5><p>As the <code>capture</code> tag is typically used in a multiline fashion, take care to strip any unwanted new lines within a <code>capture</code> tag using the hyphen as described above. If this is not done, the new lines will be included in the result of your <code>capture</code>. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% assign animal = &quot;frog&quot; %}</span></span>
<span class="line"><span>{% capture colour %}</span></span>
<span class="line"><span>{% if animal == &quot;frog&quot; %}</span></span>
<span class="line"><span>green</span></span>
<span class="line"><span>{% else %}</span></span>
<span class="line"><span>unknown</span></span>
<span class="line"><span>{% endif %}</span></span>
<span class="line"><span>{% endcapture %}</span></span>
<span class="line"><span>result=&quot;{{colour}}&quot;</span></span></code></pre></div><p>Output in text format would be:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span></span></span>
<span class="line"><span></span></span>
<span class="line"><span>result=&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>green</span></span>
<span class="line"><span></span></span>
<span class="line"><span>&quot;</span></span></code></pre></div><p>In some cases the whitespace characters may also present as their escaped sequences such as <code>\\n</code> within strings.</p><p>For more information on whitespace control, please see the <a href="https://shopify.github.io/Liquid/basics/whitespace/" target="_blank" rel="noreferrer">Shopify Liquid documentation</a>.</p><h3 id="tags-1" tabindex="-1">Tags <a class="header-anchor" href="#tags-1" aria-label="Permalink to &quot;Tags&quot;">​</a></h3><h4 id="control-flow" tabindex="-1">Control Flow <a class="header-anchor" href="#control-flow" aria-label="Permalink to &quot;Control Flow&quot;">​</a></h4><h5 id="case-when" tabindex="-1"><code>case/when</code> <a class="header-anchor" href="#case-when" aria-label="Permalink to &quot;\`case/when\`&quot;">​</a></h5><p>The Shopify Liquid documentation says that you can provide the values as a comma-separated list, or separate them using an <code>or</code> operator. This is true with our V2 Liquid engine, however V1 does not allow a comma-separated list and must instead make use of the <code>or</code> operator.</p><p>For more information on control flow, please see the <a href="https://shopify.github.io/Liquid/tags/control-flow/" target="_blank" rel="noreferrer">Shopify Liquid documentation</a>.</p><h4 id="iteration" tabindex="-1">Iteration <a class="header-anchor" href="#iteration" aria-label="Permalink to &quot;Iteration&quot;">​</a></h4><p>There are some parameters that do not work in a <code>for</code> for our V1 Liquid engine. The following parameters will only work in V2:</p><ul><li><code>offset</code></li><li><code>range</code></li></ul><p>You are also unable to use the following in either Liquid engine version:</p><ul><li><code>tablerow</code></li><li><code>tablerowloop</code></li></ul><h4 id="whitespace-considerations" tabindex="-1">Whitespace considerations <a class="header-anchor" href="#whitespace-considerations" aria-label="Permalink to &quot;Whitespace considerations&quot;">​</a></h4><p>The following <code>forloop</code> example from the Shopify Liquid documentation:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% assign smoothie_flavors = &quot;orange, strawberry, banana&quot; | split: &quot;, &quot; %}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{% for flavor in smoothie_flavors -%}</span></span>
<span class="line"><span>  {%- if forloop.length &gt; 0 -%}</span></span>
<span class="line"><span>    {{ flavor }}{% unless forloop.last %}-{% endunless -%}</span></span>
<span class="line"><span>  {%- endif -%}</span></span>
<span class="line"><span>{% endfor %}</span></span></code></pre></div><p>Results in:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>orange-strawberry-banana</span></span></code></pre></div><p>However, our V1 engine actually gives us:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    orange-    strawberry-    banana</span></span></code></pre></div><p>The following <code>cycle</code> example from the Shopify Liquid documentation:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% cycle &quot;one&quot;, &quot;two&quot;, &quot;three&quot; %}</span></span>
<span class="line"><span>{% cycle &quot;one&quot;, &quot;two&quot;, &quot;three&quot; %}</span></span>
<span class="line"><span>{% cycle &quot;one&quot;, &quot;two&quot;, &quot;three&quot; %}</span></span>
<span class="line"><span>{% cycle &quot;one&quot;, &quot;two&quot;, &quot;three&quot; %}</span></span></code></pre></div><p>Gives us:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>one</span></span>
<span class="line"><span>two</span></span>
<span class="line"><span>three</span></span>
<span class="line"><span>one</span></span></code></pre></div><p>However, again our V1 engine instead gives us something slightly different:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>onetwothreeone</span></span></code></pre></div><p>The same formatting can also be seen for <code>increment</code> and <code>decrement</code> for variables where V1 places the results on a single line instead of a new line for each.</p><h4 id="iterating-over-strings" tabindex="-1">Iterating over strings <a class="header-anchor" href="#iterating-over-strings" aria-label="Permalink to &quot;Iterating over strings&quot;">​</a></h4><p>Unlike the examples above, this is not just a whitespace formatting difference - our two Liquid engines actually run the <code>for</code> loop a different number of times when the thing being iterated is a string, because our V1 and V2 Liquid engines use different versions of the underlying Fluid library, and this behaviour changed between those versions.</p><p>Given the following Liquid, where <code>input.interests[0]</code> is the string <code>&quot;test&quot;</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% for letter in input.interests[0] %}</span></span>
<span class="line"><span> {{ letter }}</span></span>
<span class="line"><span>{% endfor %}</span></span></code></pre></div><p><strong>V1</strong> runs the loop once <em>per character</em> in the string (4 times for <code>&quot;test&quot;</code>), giving you:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>t</span></span>
<span class="line"><span>e</span></span>
<span class="line"><span>s</span></span>
<span class="line"><span>t</span></span></code></pre></div><p><strong>V2</strong> runs the loop only once, treating the whole string as a single item, giving you:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>test</span></span></code></pre></div><p>Even though V1 is genuinely running 4 loop iterations to V2&#39;s 1, adding hyphens to strip the whitespace between each of V1&#39;s iterations happens to produce the same end result, since concatenating the 4 individually-printed characters with no whitespace between them gives you the same string as printing it all at once:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% for letter in input.interests[0] %}</span></span>
<span class="line"><span> {{- letter -}}</span></span>
<span class="line"><span>{% endfor %}</span></span></code></pre></div><p>Results in <code>test</code> for both Liquid engines.</p><p>If you wanted the behaviour of V1 (iterating character by character) in V2, you can split the string into individual characters first:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% assign chars = input.interests[0] | split: &quot;&quot; %}</span></span>
<span class="line"><span>{% for letter in chars %}</span></span>
<span class="line"><span>{{ letter -}}</span></span>
<span class="line"><span>{% endfor %}</span></span></code></pre></div><p>For more information on iteration, please see the <a href="https://shopify.github.io/Liquid/tags/iteration/" target="_blank" rel="noreferrer">Shopify Liquid documentation</a>.</p><h3 id="filters-1" tabindex="-1">Filters <a class="header-anchor" href="#filters-1" aria-label="Permalink to &quot;Filters&quot;">​</a></h3><h4 id="newline-to-br" tabindex="-1"><code>newline_to_br</code> <a class="header-anchor" href="#newline-to-br" aria-label="Permalink to &quot;\`newline_to_br\`&quot;">​</a></h4><p>The following example from the Shopify Liquid documentation:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% capture string_with_newlines %}</span></span>
<span class="line"><span>Hello</span></span>
<span class="line"><span>there</span></span>
<span class="line"><span>{% endcapture %}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{{ string_with_newlines | newline_to_br }}</span></span></code></pre></div><p>Results in:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span></span></span>
<span class="line"><span></span></span>
<span class="line"><span>&lt;br /&gt;</span></span>
<span class="line"><span>Hello&lt;br /&gt;</span></span>
<span class="line"><span>there&lt;br /&gt;</span></span></code></pre></div><p>However, for V1 we get:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Hello&lt;br /&gt;there&lt;br /&gt;</span></span></code></pre></div><p>And for V2 we get:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span></span></span>
<span class="line"><span></span></span>
<span class="line"><span>&lt;br /&gt;Hello&lt;br /&gt;there&lt;br /&gt;</span></span></code></pre></div><p>So for both versions, the result ends up on a single line instead of multiple lines. Also, for V1 the first <code>&lt;br /&gt;</code> is not present at all.</p><p>The following filters do not work in our V1 Liquid engine:</p><ul><li><code>remove_first</code></li><li><code>remove_last</code></li><li><code>replace_first</code></li><li><code>replace_last</code></li><li><code>sum</code></li></ul><p>e.g. <code>remove_first</code> for the following:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{{ &quot;I strained to see the train through the rain&quot; | remove_first: &quot;rain&quot; }}</span></span></code></pre></div><p>results in <code>I strained to see the train through the rain</code> instead of the expected <code>I sted to see the train through the rain</code>. These filters are essentially ignored and will just print out whatever is defined before the filter e.g.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% assign total_quantity = input.collection.products | sum: &quot;quantity&quot; %}</span></span></code></pre></div><p>Gives us:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[{&quot;title&quot;:&quot;hat&quot;,&quot;quantity&quot;:1},{&quot;title&quot;:&quot;shirts&quot;,&quot;quantity&quot;:3},{&quot;title&quot;:&quot;pants&quot;,&quot;quantity&quot;:2}]</span></span></code></pre></div><p>Instead of an expected sum value of <code>6</code></p><p>These filters work as expected in the Shopify Liquid documentation for our V2 Liquid engine.</p><h4 id="default" tabindex="-1"><code>default</code> <a class="header-anchor" href="#default" aria-label="Permalink to &quot;\`default\`&quot;">​</a></h4><p>When setting a value to an empty string and using <code>default</code> you will get different behaviour depending on the Liquid engine version used. If you had:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{{ product_price | default: 2.99 }}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{% assign product_price = 4.99 %}</span></span>
<span class="line"><span>{{ product_price | default: 2.99 }}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{% assign product_price = &quot;&quot; %}</span></span>
<span class="line"><span>{{ product_price | default: 2.99 }}</span></span></code></pre></div><p>You would see this in V1:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>2.99</span></span>
<span class="line"><span></span></span>
<span class="line"><span>4.99</span></span></code></pre></div><p>Whereas for V2, you would get a more accurate result of:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>2.99</span></span>
<span class="line"><span></span></span>
<span class="line"><span>4.99</span></span>
<span class="line"><span></span></span>
<span class="line"><span>2.99</span></span></code></pre></div><p>Therefore, in V1 an empty string would be considered a valid value which didn&#39;t fall back to the default, whereas V2 now closely resembles the Shopify Liquid documentation and does fall back to the default value when the value is an empty string.</p><h4 id="forloop-indexing" tabindex="-1"><code>forloop</code> Indexing <a class="header-anchor" href="#forloop-indexing" aria-label="Permalink to &quot;\`forloop\` Indexing&quot;">​</a></h4><p>This behaves slightly differently depending on the Liquid engine used. In V1 you can do <code>forloop.index - 1</code> like so:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% for val in instances[0].values %}</span></span>
<span class="line"><span>  {% assign index = forloop.index - 1 %}</span></span>
<span class="line"><span>	...</span></span>
<span class="line"><span>{% unless forloop.last %},{% endunless %}</span></span></code></pre></div><p>However, this will not work in V2. You must instead use <code>forloop.index0</code> to achieve the same result:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% for val in instances[0].values %}</span></span>
<span class="line"><span>  {% assign index = forloop.index0 %}</span></span>
<span class="line"><span>  ...</span></span>
<span class="line"><span>{% unless forloop.last %},{% endunless %}</span></span></code></pre></div><p><code>forloop.index0</code> can be used in V1 too, therefore it is recommended to use this approach throughout.</p><h3 id="strict-types" tabindex="-1">Strict types <a class="header-anchor" href="#strict-types" aria-label="Permalink to &quot;Strict types&quot;">​</a></h3><p>Our V2 Liquid engine is stricter than V1 when it comes to property types. A common example is when comparing <code>true</code> with <code>&quot;true&quot;</code>. If you had the following:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% if testStringBool == true %}</span></span></code></pre></div><p>Where <code>testStringBool</code> is <code>&quot;true&quot;</code>, then V1 would return a successful match. However, for V2 because <code>&quot;true&quot;</code> is a string, this would not match the boolean value of <code>true</code>. To get the same result in V2 you could do:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% if testStringBool == &quot;true&quot; %}</span></span></code></pre></div><p>or even</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% if testStringBool == true or testStringBool == &quot;true&quot; %}</span></span></code></pre></div><p>to have both scenarios covered.</p><h3 id="precomputing-values" tabindex="-1">Precomputing values <a class="header-anchor" href="#precomputing-values" aria-label="Permalink to &quot;Precomputing values&quot;">​</a></h3><p>A common approach in V1 would be to compute a value as part of an <code>if</code> statement, like so:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% if resource.id == resource | split: &quot;/&quot; | last %}</span></span></code></pre></div><p>However, due to the order of operations in our V2 Liquid engine, this will not be possible and you should instead precompute your value and then use this in the <code>if</code> statement instead, like so:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% assign resource_id = resource | split: &quot;/&quot; | last %}</span></span>
<span class="line"><span>{% if resource.id == resource_id %}</span></span></code></pre></div><h3 id="xml-single-vs-multiple-elements" tabindex="-1">XML single vs multiple elements <a class="header-anchor" href="#xml-single-vs-multiple-elements" aria-label="Permalink to &quot;XML single vs multiple elements&quot;">​</a></h3><p>When mapping XML input (see <a href="/docs/quickstart/liquid.html#xml-input-mapping">XML input mapping</a>), if there is more than one element with the same name at the same level, they are combined into an array e.g. two <code>&lt;Item&gt;</code> elements under the same parent let you do <span><code>{{ input.Item.size }}</code></span>, <span><code>{{ input.Item.first }}</code></span>, or <code>{% for item in input.Item %}</code>.</p><p>If there is only a <em>single</em> occurrence of that element, our two Liquid engines behave differently:</p><ul><li><strong>V1</strong> - the single element is not treated as an array, so <span><code>{{ input.Item.size }}</code></span>, <span><code>{{ input.Item.first }}</code></span>, <span><code>{{ input.Item.last }}</code></span> and <code>{% for item in input.Item %}</code> will all return nothing.</li><li><strong>V2</strong> - the single element is still treated as if it were a one-item array, so <span><code>{{ input.Item.size }}</code></span> returns <code>1</code>, and <span><code>{{ input.Item.first }}</code></span>/<span><code>{{ input.Item.last }}</code></span>/<code>{% for item in input.Item %}</code> all behave the same as they would for multiple elements.</li></ul><p>If you need Liquid that works the same way regardless of whether there is one element or several, and you need to support V1, you can use a workaround like this:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% assign items = input.Item %}</span></span>
<span class="line"><span>{% if items.first %}</span></span>
<span class="line"><span>  {% for item in items %}...{% endfor %}</span></span>
<span class="line"><span>{% else %}</span></span>
<span class="line"><span>  ...access \`items\` directly here instead of \`items.first\`, as there is only a single item...</span></span>
<span class="line"><span>{% endif %}</span></span></code></pre></div><p>For V2 this workaround is unnecessary (though it will still work, since <code>items.first</code> is always truthy there) as the single-item case already behaves the same as the multiple-item case.</p><h4 id="looping-over-a-single-element-while-also-accessing-its-nested-fields" tabindex="-1">Looping over a single element while also accessing its nested fields <a class="header-anchor" href="#looping-over-a-single-element-while-also-accessing-its-nested-fields" aria-label="Permalink to &quot;Looping over a single element while also accessing its nested fields&quot;">​</a></h4><p>Because V2 treats a single element as a one-item array, you can loop over a container even when it only occurs once, while still accessing its own nested array and sibling fields from inside that loop - this is not possible in V1 at all. Given the following XML:</p><div class="language-xml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">xml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;?</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">xml</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> version</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1.0&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> encoding</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;utf-8&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">?&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;Fox Mulder&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">interests</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">interest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;paranormal&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">interest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">interest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;paranormal 2&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">interest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">different-thing</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;this is different&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">different-thing</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">interests</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">interestsSingle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        hello</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">interestsSingle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p>Note that <code>interests</code> only occurs once, and the following Liquid loops over it while also accessing its <code>interest</code> array and <code>different-thing</code> sibling field from inside that loop:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{% for a in input.interests %}</span></span>
<span class="line"><span>{% for b in a.interest %}</span></span>
<span class="line"><span>{{ b }}</span></span>
<span class="line"><span>{% endfor %}</span></span>
<span class="line"><span>{{ a.different-thing }}</span></span>
<span class="line"><span>{% endfor %}</span></span></code></pre></div><p><strong>V1</strong> gives you nothing at all here - the outer <code>{% for a in input.interests %}</code> loop never runs, since <code>interests</code> only occurs once and V1 does not treat a single element as an array.</p><p><strong>V2</strong> gives you:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>paranormal</span></span>
<span class="line"><span>paranormal 2</span></span>
<span class="line"><span>this is different</span></span></code></pre></div><p>since the outer loop runs once (treating the single <code>interests</code> element as a one-item array), letting you access its <code>interest</code> array and <code>different-thing</code> field from within the loop body, exactly as if there had been multiple <code>interests</code> elements to loop over.</p><h4 id="field-names-that-clash-with-size-first-or-last" tabindex="-1">Field names that clash with <code>size</code>, <code>first</code> or <code>last</code> <a class="header-anchor" href="#field-names-that-clash-with-size-first-or-last" aria-label="Permalink to &quot;Field names that clash with \`size\`, \`first\` or \`last\`&quot;">​</a></h4><p>If the XML element itself has a child element or attribute literally named <code>size</code>, <code>first</code> or <code>last</code>, that real field always takes precedence over the array-style behaviour described above in V2 e.g. if a single <code>&lt;Item&gt;</code> element has a child <code>&lt;size&gt;</code> element, <span><code>{{ input.Item.size }}</code></span> would return the value of that <code>&lt;size&gt;</code> element rather than the count of <code>Item</code> elements. This matches how Liquid resolves properties generally - if the underlying object defines its own property with that name, it is used instead of any built-in behaviour.</p><h3 id="aireglu-defined-properties" tabindex="-1">AireGlu Defined Properties <a class="header-anchor" href="#aireglu-defined-properties" aria-label="Permalink to &quot;AireGlu Defined Properties&quot;">​</a></h3><p>There are some differences between V1 and V2 when it comes to casing. For example, for V1 you would access an endpoint task status code like so <span><code>{{ endpoint.Tasks[0].StatusCode }}</code></span>, however for V2 you must now use <code>camelCase</code> e.g. <span><code>{{ endpoint.tasks[0].statusCode }}</code></span></p><p>For more information on the AireGlu data you can access, please see the <a href="/docs/quickstart/liquid.html#data-shape">Data Shape</a> section of our Liquid docs. You can also find the receivers data shape <a href="/docs/getting-started/receivers.html#shaping-the-data">here</a>.</p>`,210),o=[t];function p(l,r,d,c,h,u){return a(),e("div",null,o)}const f=s(i,[["render",p]]);export{m as __pageData,f as default};
